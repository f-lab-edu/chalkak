cors:
  allowed-origins: http://localhost:3000

file:
  upload-dir: build/test-uploads

spring:
  datasource:
    url: jdbc:h2:mem:chalkak;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
    hikari:
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 50

logging:
  level:
    com.chalkak: debug

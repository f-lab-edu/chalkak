spring:
  application:
    name: chalkak
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: prometheus
  metrics:
    tags:
      application: ${spring.application.name}
